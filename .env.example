# Deployed RitualArena address — written automatically by `bun run deploy:contract`.
NEXT_PUBLIC_ARENA_ADDRESS=

# Optional RPC override (defaults to https://rpc.ritualfoundation.org).
# NEXT_PUBLIC_RITUAL_RPC_URL=

# Deploy only — your testnet deployer key. Pass inline instead of committing:
#   PRIVATE_KEY=0x... bun run deploy:contract
# PRIVATE_KEY=
